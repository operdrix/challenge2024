{% extends 'base.html.twig' %}

{% block title %}Grade{% endblock %}

{% block body %}
    <h1>Grade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grade.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ grade.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_grade_index', {'school_id': grade.school.id}) }}">back to list</a>

    <a href="{{ path('app_grade_edit', {'school_id': grade.school.id, 'id': grade.id}) }}">edit</a>

    {{ include('grade/_delete_form.html.twig') }}
{% endblock %}
