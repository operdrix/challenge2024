<!-- Colonne de droite -->
<div class="col-span-1">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="flex justify-center py-2">
            <button type="submit" class="text-white bg-primary-700 hover:bg-primary-800 focus:ring-4 focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800">{{ button_label|default('Save') }}</button>
        </div>
    {{ form_end(form) }}
</div>
